packages:
  yum:
    postgresql93-devel: []

commands:
  add_bundle_exec:

container_commands:
  01deploy:
    command: rake db:schema:load
    command: rake db:seed
    command: rake sunspot:solr:start